# Production Environment Variables for Synology NAS / Docker
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env files with real secrets!

# Database Password (generate a strong password)
POSTGRES_PASSWORD=your-secure-database-password-here

# Session Secret (generate a random string, min 32 characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-min-32-chars-here

# Server Configuration
# Set to your public domain for proper OAuth callback URL construction
# This is used by the app to build correct redirect URIs in Docker/reverse-proxy setups
HOST=resolutions.yourdomain.com

# OAuth - Google (get from https://console.cloud.google.com/apis/credentials)
# Callback URL: https://resolutions.yourdomain.com/api/callback?provider=google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth - GitHub (get from https://github.com/settings/developers)
# Callback URL: https://resolutions.yourdomain.com/api/callback/github
# IMPORTANT: GitHub only supports ONE callback - create SEPARATE app for prod!
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth - Apple (get from https://developer.apple.com)
# Callback URL: https://resolutions.yourdomain.com/api/callback?provider=apple
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# AI Model API Keys
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=

# AI Configuration
AI_STRATEGY=all
AI_ENABLE_ANALYSIS=true
AI_MAX_HISTORICAL_CHECKINS=5
