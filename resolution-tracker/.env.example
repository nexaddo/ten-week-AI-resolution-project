# Database Connection
# PostgreSQL connection string for local development
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/resolutions

# Session Secret (used for session encryption)
# Must be set for secure sessions. Generate a random string:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# For development, you can use a simple string:
SESSION_SECRET=your-dev-secret-key-change-in-production

# OAuth Configuration (OpenID Connect / OAuth 2.0)
# Choose one or more providers and set their credentials.
# Callback URL for local dev: http://localhost:5000/api/callback?provider=<provider>

# Google OIDC
# GOOGLE_CLIENT_ID=your-google-client-id-here
# GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Apple OIDC
# APPLE_CLIENT_ID=your-apple-service-id-here
# APPLE_CLIENT_SECRET=your-apple-client-secret-jwt-here

# GitHub OAuth (non-OIDC)
# GITHUB_CLIENT_ID=your-github-client-id-here
# GITHUB_CLIENT_SECRET=your-github-client-secret-here

# Replit OIDC (if deploying to Replit)
# REPL_ID=your-replit-id

# Optional default provider if multiple are configured
# DEFAULT_AUTH_PROVIDER=google

# Legacy OIDC configuration (still supported for custom providers)
# ISSUER_URL=https://accounts.google.com
# CLIENT_ID=your-legacy-client-id-here
# CLIENT_SECRET=your-legacy-client-secret-here

# Node Environment
NODE_ENV=development

# Port (optional, defaults to 5000)
# PORT=5000
