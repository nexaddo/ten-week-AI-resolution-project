# Database Connection
# PostgreSQL connection string for local development
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/resolutions

# Session Secret (used for session encryption)
# Must be set for secure sessions. Generate a random string:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# For development, you can use a simple string:
SESSION_SECRET=your-dev-secret-key-change-in-production

# OIDC Configuration (OpenID Connect / OAuth 2.0)
# For Google OAuth, set these values:
# 1. Create a project at https://console.cloud.google.com
# 2. Create OAuth 2.0 credentials (Web application)
# 3. Add http://localhost:5000/api/callback to authorized redirect URIs
# 4. Copy your Client ID and Client Secret below

# OIDC Issuer URL (the provider's OIDC endpoint)
# Google: https://accounts.google.com
# Microsoft: https://login.microsoftonline.com/common
# Replit: https://replit.com/oidc
# ISSUER_URL=https://accounts.google.com

# OAuth Client ID (your application's client ID from the provider)
# CLIENT_ID=your-google-client-id-here

# OAuth Client Secret (keep this secure, never commit to git!)
# CLIENT_SECRET=your-google-client-secret-here

# Replit-specific configuration (if deploying to Replit)
# REPL_ID=your-replit-id

# Node Environment
NODE_ENV=development

# Port (optional, defaults to 5000)
# PORT=5000
