# Database Connection
# PostgreSQL connection string for local development
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/resolutions

# Session Secret (used for session encryption)
# Must be set for secure sessions. Generate a random string:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# For development, you can use a simple string:
SESSION_SECRET=your-dev-secret-key-change-in-production

# OAuth Configuration (OpenID Connect / OAuth 2.0)
# Choose one or more providers and set their credentials.
# 
# IMPORTANT: Create SEPARATE OAuth apps for development vs production!
# - GitHub only allows ONE callback URL per OAuth app
# - Google/Apple support multiple callbacks (can use same app)
#
# Callback URLs for LOCAL DEVELOPMENT:
# Direct dev (npm run dev) uses port 5000
# Docker dev (docker compose up) uses port 5002
# - Google:  http://localhost:<port>/api/callback?provider=google
# - GitHub:  http://localhost:<port>/api/callback/github
# - Apple:   http://localhost:<port>/api/callback?provider=apple

# Google OIDC (supports multiple callback URLs - can reuse prod app)
# GOOGLE_CLIENT_ID=your-google-client-id-here
# GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Apple OIDC (supports multiple callback URLs - can reuse prod app)
# APPLE_CLIENT_ID=your-apple-service-id-here
# APPLE_CLIENT_SECRET=your-apple-client-secret-jwt-here

# GitHub OAuth (ONE callback only - create separate DEV app)
# Get from: https://github.com/settings/developers
# Callback: http://localhost:5000/api/callback/github (direct dev)
#       or: http://localhost:5002/api/callback/github (Docker dev)
# GITHUB_CLIENT_ID=your-github-DEV-client-id-here
# GITHUB_CLIENT_SECRET=your-github-DEV-client-secret-here



# Optional default provider if multiple are configured
# DEFAULT_AUTH_PROVIDER=google

# Legacy OIDC configuration (still supported for custom providers)
# ISSUER_URL=https://accounts.google.com
# CLIENT_ID=your-legacy-client-id-here
# CLIENT_SECRET=your-legacy-client-secret-here

# Node Environment
NODE_ENV=development

# Port (optional, defaults to 5000)
# PORT=5000

# AI Model API Keys (for check-in analysis)
# Get your API keys from:
# - Anthropic: https://console.anthropic.com/
# - OpenAI: https://platform.openai.com/api-keys
# - Google: https://makersuite.google.com/app/apikey

# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here
# GOOGLE_AI_API_KEY=your-google-ai-api-key-here

# AI Configuration
# Strategy: "all" (call all models), "rotate" (round-robin), "single" (one model only)
# AI_STRATEGY=all
# AI_DEFAULT_MODEL=anthropic
# AI_ENABLE_ANALYSIS=true
# AI_MAX_HISTORICAL_CHECKINS=5
